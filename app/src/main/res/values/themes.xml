<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Wallet" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Remove color overrides so buttons and UI components follow default styling -->
        <!-- Button Colors -->
        <item name="colorPrimaryContainer">@color/black</item>
        <item name="colorOnPrimaryContainer">@color/white</item>
    </style>

    <!-- Light Theme -->
    <style name="Theme.Wallet.Light" parent="Base.Theme.Wallet" />

    <!-- Default Theme -->
    <style name="Theme.Wallet" parent="Base.Theme.Wallet" />


</resources>
